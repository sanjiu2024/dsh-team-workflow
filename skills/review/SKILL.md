---
name: review
description: 'Use when 只审查当前 git diff 的正确性、不改代码。按严重程度列出逻辑错误、破坏性行为、缺失的验证。'
---

# 审查当前改动

只审查当前 git diff 的正确性，不管代码风格。

按严重程度排：

1. **逻辑错误** — 边界条件、空值、并发、错误处理被吞掉
2. **破坏性行为** — 有没有改动别人的调用方、有没有可能丢数据
3. **缺失的验证** — 新逻辑有没有对应的可运行检查

每条给出 `文件:行号` 和一句具体的修法。没发现的类别直接说「无」。

最后一行写：`结论：可以合 / 需要改 <具体哪条>`。
