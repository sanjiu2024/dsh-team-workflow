# REQ-NNN · <一句话标题>

> 复制这份文件为 `REQ-NNN-<短主题>.md`（NNN 三位递增，如 `REQ-001-worktree隔离.md`）。
> 填完再动手。**开发期间对着这份文档写代码，收尾时逐条回来打勾。**
> 写不出来「怎么知道做完了」的，说明需求还没想清楚 —— 那就先问，别先写。

- **状态**：草稿 / 进行中 / 已完成 / 放弃
- **对应版本**：`<从> → <到>`（加能力 minor / 只修 bug patch / 不兼容 major）
- **提出**：<谁、什么时候、什么场景下提的>

---

## 1. 要做什么

<用户视角的诉求。用他自己会说的话，别翻译成实现语言。
如果是从某次对话里来的，把原话引上 —— 后面「不做什么」的判断要靠它。>

## 2. 为什么（不做的后果）

<不做会怎样。如果答不出来，就别做（YAGNI）。>

## 3. 不做什么

<明确划出边界。这节和「要做什么」一样重要 —— 没写边界的需求会无限膨胀。
包括：这次不碰的相邻功能、暂不支持的情形、已知会留下的天花板。>

## 4. 验收标准

<**必须是可执行的检查**，不是形容词。写完问自己：这条怎么跑？>

- [ ] <例：`npm test` 全绿，新增第 N 个自检>
- [ ] <例：真跑一次 `xxx`，输出包含 `yyy`>
- [ ] <例：反向验证 —— 把实现里的 Z 去掉，测试必须变红>
- [ ] <例：`docs/` 某文档更新，`package.json` 版本 = `CHANGELOG.md` 首个标题>

## 5. 已知约束（侦察出来的硬事实）

<动手前查明的「做不到」和「只能这样」。每条带证据（`文件:行号`）。
这一节是防止后来者（包括三个月后的你）重新踩一遍。>

| 约束 | 证据 | 影响 |
| --- | --- | --- |
| <例：子代理 cwd 无法指定> | <`包/lib/index.js:505`> | <方案只能走 X> |

## 6. 方案

<怎么做。含：改哪些文件、新增哪些、为什么不选别的路。
如果方案拿不准（存在真实分歧且走错代价大），在这里写明候选与取舍。>

## 7. 天花板（做完之后仍不支持的）

<诚实列出。不写这一节，用户会以为它全能。>

## 8. 验收记录

<收尾时填。每条验收标准对应：实际命令 + 实际输出摘要。
**没有真实运行结果的，不要勾。**>

- [ ] … → <命令> → <结果>
