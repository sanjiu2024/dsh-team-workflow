# 用量日报判读 — 2026-09-25

> 数字全部引自同日 `2026-09-25.md`（脚本产出），原文保留、未重算。
> 标注「补充证据」的是判读时用 node 定向抽取审计日志得到的**归属**信息（按会话聚合错误、定位重灾区读取的来源会话），只做归因，不替代报告数字。

## 慢在哪一步

瓶颈在模型侧：模型等待 11,857,327ms，占步墙钟（15,635,110ms）的 **75.8%**；工具执行 24.1%（3,774,450ms），其他≈0（5,367ms）。最慢一步 197,732ms 里 196,153ms 在等模型。

最慢 10 步（报告 §3）里 5 步模型等待主导；另 5 步工具侧主导，其中 3 步是 `ask_user_question`（109,763 / 60,574 / 55,507ms）——那是在**等人回答**，不是工具慢；真正值得看的是 2 步 `pwsh`（77,148 / 62,131ms），单条命令超过 1 分钟，符合报告 §11「慢工具后台化」的画像。

各工具 p90 对中位数的偏离：报告的耗时分解只给了整体 p50/p90/p99，没有按工具拆分 —— **数据不足**，不下结论。

## token 耗在哪一步

计费输入 86,016,689 tokens，缓存命中率 95.1%。放大表（报告 §4）：约 **90.6%**（粗估 77,925,847 tokens）来自工具结果在后续各步的重复携带，最重四项：

| 工具 | 放大后 tokens |
| --- | ---: |
| `read` | 39,174,000（占计费输入 45.5%，报告 §11） |
| `pwsh` | 22,916,173 |
| `(未记录工具名)` | 8,157,440 |
| `grep` | 6,916,184 |

重复读（报告 §7）：重复调用合计浪费约 68,517 字符，最重一条 `read` 同参数调了 4 次；同一文件重复读取 top：`scripts\usage-report.mjs` 26 次（同参数 2）、`~\AppData\Local\Temp\big.txt` 19 次、`vendor\pi-magic-context\dist\index.js` 11 次、`scripts\selftest-mc.mjs` 7+4 次、`Temp\cr2.txt` 7 次（同参数 6）。

**归因（补充证据）**：重灾区集中在开发/测试会话 —— `usage-report.mjs` 的调用散布在 11 个会话（含 3 个 `task-*` 定时任务会话），`big.txt`/`cr2.txt`/`selftest-mc.mjs` 集中在少数几个会话；报告 §6 的重复搜索词（`vue3` 4 会话 4 次等）也是短会话探活流量。**昨天的「浪费」很大一块是这套工具链在测自己，不是日常稳态行为。** 稳态里真正可修的收窄为两类：同参数重读（同一区段读两遍），以及单条超大工具结果（read 最大 58,335 字符、web_fetch 最大 30,895 字符）。

数据质量注：`(未记录工具名)` 放大后 8,157,440 tokens、75 条结果没记下工具名（报告 §9），这块去向不明 —— 审计日志补工具名属 lib/，另行建议。

## 值得加的功能

1. **web_fetch 纳入 rtk 压缩白名单** —— 落点：`team/extensions/rtk.json` ✅ 当天已做
   - 依据：`web_fetch` 平均 10,371 字符/次、最大 30,895 字符（粗估 8,827 tokens 一次性进上下文），rtk 压缩列 = 否（报告 §5、§10）。
   - 可行性已核实：`lib/rtk.js:167-179` 的 `tools/post-execute` 钩子按工具名匹配 `compactTools`（大小写不敏感），对 web_fetch 这类非 shell 工具同样生效；`RTK_FIELDS.compactTools`（lib/rtk.js:56）接受字符串数组。**注意 config.compactTools 是整组替换**（lib/rtk.js:176-178），必须带上原默认 6 个工具，否则 bash/grep 等会丢压缩。
2. **read 分页纪律写进基线** —— 落点：`team/RULES.md`（本次未动，留观察）
   - 依据：read 放大后 39,174,000 tokens 占计费输入 45.5%（报告 §11）；同参数重读最多 4 次。
   - 建议：在 RULES.md 代码节加一行「读大文件先 grep 定位再 read，带 offset/limit，同一区段不重读」。不动手的原因：重读多为测试流量、一天样本不足以改全团队基线；`read` 不进压缩白名单是刻意设计（截断会断行号，lib/rtk.js:38-40），行为纪律是唯一杠杆。
3. **ctx_* 工具系统性报错待查** —— 落点：`lib/mc-adapter.js` 侧（禁改区，只提建议）
   - 依据（补充证据）：当天 ctx_*/skill 类错误共 45 条（与报告 §5 各工具错误数吻合），其中 **38 条共享同一错误结果**（67 字符、同一 hash `8603acc1…`），横跨 ctx_memory/ctx_note/ctx_search/ctx_expand/skill 与十几个会话 —— 是**系统性原因**（疑似某类会话下这组工具整体不可用），不是调用姿势问题；报告 §10「参数或路径写法的固定毛病」的猜测判读不准。
   - 建议：先取到那 67 字符报错原文再定位修哪层；属 lib/，本任务不动。
4. **压缩水位继续观察** —— 落点：`team/agent-settings.json`（本次不动）
   - 依据：单次峰值 prompt 120,887 tokens = 窗口 94.4%（报告 §2）。但该文件注释说明 dsh 兜底阈值抬到 ~90% 是刻意设计（magic-context historian 在 65% 日常折叠）；1,584 次请求里一个峰值样本不足以推翻设计。连续多日顶格再调。
5. **搜索查询缓存** —— 落点：rtk / dsh 层（不在 team/skills 可改范围，只提建议）
   - 依据：同一组词最多 4 个会话各搜一次（`vue3`，累计 17,391 字符，报告 §6）。重复搜索多来自测试流量，跨会话缓存的真实收益待稳态数据确认。

## 顺手修记录

- 改动：`team/extensions/rtk.json` 增加 `compactTools`（原默认 6 个 + `web_fetch`），web_fetch 大结果将按 lib/rtk.js `compactText` 头 70% + 尾 30% 截断（maxChars 12,000 / maxLines 220 / minChars 1,000）。依据 = 上面第 1 条。
- 边界：只碰 team/ 下 1 个文件；lib/、scripts/ 未改。
- 验证：`node scripts/selftest.mjs` 通过（结果见提交）。

## 本日结论

计费输入 86,016,689 tokens、缓存命中率 95.1%；墙钟瓶颈在模型等待（75.8%），token 大头是工具结果的上下文放大（90.6%，read 39,174,000 / pwsh 22,916,173 tokens）。当天流量里开发与测试占比很高，重灾区重复读与重复搜索多为「工具链测自己」。顺手把 web_fetch 纳入 rtk 压缩白名单；read 分页纪律、ctx_* 系统性报错（38 条同 hash）、压缩水位留观察或写成建议。
